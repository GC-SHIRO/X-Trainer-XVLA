# X-VLA LoRA 训练与真机部署实施方案

更新日期：2026-09-14

状态：第 4、5、6 节已新增 LoRA 配置、参数分组、可选分阶段调度和独立启动脚本；合并导出和端到端验证尚未实施。除明确标注已新增的文件外，本文接口仍为设计建议。

## 1. 目标与已确认需求

在现有 LeRobot / X-Trainer 工程中增加独立、可选的 X-VLA LoRA 微调能力：

1. 从官方原始 X-VLA checkpoint 开始训练，不以已经全量微调的模型作为基座。
2. 使用训练机器上的本地模型目录，路径可配置，不固定为 `models/xvla-base`。
3. 新配置参考现有 `configs/xtrainer/train_xvla.yaml`，保留机器人数据约定。
4. 增加独立、可选的 LoRA 分阶段学习率调度，不改变原有全量训练行为。
5. 训练后将 adapter 与同一个原始基座合并，导出完整模型。
6. 导出模型复用现有真机部署代码，不要求部署端动态加载 adapter。
7. 保留已经部署成功的全量微调模型，作为效果对照和回退版本。

当前仅编写方案文档。训练配置、训练入口、优化器、调度器和部署代码均不在本次文档任务中修改。

## 2. 模型版本与数据流

为避免混淆，区分以下产物：

| 名称 | 含义 | 用途 |
| --- | --- | --- |
| A：原始基座 | 本地官方原始 X-VLA checkpoint | LoRA 初始化和合并 |
| B：已有全量模型 | 已全量微调并部署成功的模型 | 对照实验和部署回退 |
| L：LoRA 产物 | Adapter 和额外完整训练模块 | 续训、归档和合并 |
| C：合并模型 | A 与 L 合并后导出的完整 checkpoint | 现有真机部署入口加载 |

```text
本地官方原始基座 A + X-Trainer 数据
                  |
                  v
             LoRA 训练
                  |
                  v
       Adapter + 额外训练模块 L
                  |
       与原始基座 A 合并导出
                  |
                  v
          独立完整模型 C
                  |
                  v
          现有真机部署代码

已有全量模型 B：独立保留，用于对照和回退
```

约束：

- L 必须与其训练时使用的 A 配套，不能直接合并到 B 或另一个同名但权重不同的基座。
- 不能覆盖 A、B 或已有训练输出目录。
- 如果 C 效果不满足要求，部署路径切回 B；不需要删除 LoRA 产物。
- 本地目录名不是模型身份，需记录基座配置及权重文件的标识或校验信息。

## 3. 当前工程基础

以下是已阅读代码中可复用的部分，不表示 LoRA 全流程已经验证通过。

| 文件 | 当前作用 | 本次设计关系 |
| --- | --- | --- |
| `configs/xtrainer/train_xvla.yaml` | X-Trainer 全量微调配置 | 作为数据和模型配置参考，不直接改成 LoRA |
| `scripts/xtrainer/train_xvla.sh` | 数据校验和训练启动 | 参考其流程，处理模型路径优先级 |
| `src/lerobot/scripts/lerobot_train.py` | 通用训练循环，已有 PEFT 入口 | 优先复用，不另写完整训练循环 |
| `src/lerobot/policies/pretrained.py` | 通用 PEFT 包装和配置转换 | 复用目标模块和额外训练模块配置 |
| `src/lerobot/policies/xvla/configuration_xvla.py` | X-VLA 配置及训练预设 | 检查新配置与预训练配置的合并规则 |
| `src/lerobot/policies/xvla/modeling_xvla.py` | 模型、冻结策略及加载逻辑 | 验证包装、恢复和完整模型加载 |
| `src/lerobot/policies/xvla/soft_transformer.py` | 动作 Transformer 和域相关模块 | 核对 LoRA 注入及完整训练模块 |
| `src/lerobot/optim/optimizers.py` | X-VLA 参数分组 | 设计 LoRA 专用分组 |
| `deploy/xtrainer/xvla_policy.py` | 真机侧 X-VLA 模型加载 | 作为合并模型的兼容性验收入口 |
| `configs/xtrainer/deploy.yaml` | 真机部署配置 | 最终按现有字段切换模型路径 |

现有启动脚本包含默认本地路径 `${REPO_ROOT}/models/xvla-base`。当该目录存在 `config.json` 时，脚本会传入模型路径覆盖配置。这个默认值不代表本地模型目录实际存在。

新 LoRA 启动路径必须尊重用户明确指定的配置，不能被上述默认目录静默替换。

## 4. 配置设计

### 4.1 独立配置文件

已新增：

```text
configs/xtrainer/train_xvla_lora.yaml
```

原全量配置保持不变。新配置以现有配置为参考，但不能直接复制后认为所有训练参数都适合 LoRA。

配置阶段说明：

- 基座、tokenizer 和数据集使用 `/path/to/...` 占位路径，必须在训练机器上替换。
- 只使用当前解析器支持的字段；`bias` 未在本地 `PeftConfig` 暴露，因此不添加未知 YAML 字段，采用 PEFT LoRA 默认值，实际包装时仍需验证。
- 当前配置保留现有训练预设，不代表第 5 节专用分阶段调度已经实现。
- 当前不要通过原 `train_xvla.sh` 启动此配置，以免其默认本地模型检测覆盖 `policy.path`。启动脚本优先级修复留在第 6 节实施。
- 相对路径按进程工作目录解析，建议填写绝对路径；路径校验及日志增强留在启动入口实施。
- `tests/xtrainer/test_train_xvla_lora_config.py` 检查配置解析、PEFT 字段和与全量配置的机器人契约一致性，不加载真实模型。

### 4.2 参考并核对的现有配置

| 类别 | 现有参考值 | 处理方式 |
| --- | --- | --- |
| 数据格式 | LeRobot Dataset v2.1 | 沿用，启动前校验 |
| 图像映射 | top、left_wrist、right_wrist | 保持训练和部署一致 |
| 动作模式 | `auto` | 初版保持，核对基座兼容性 |
| 域编号 | `19` | 沿用前确认域使用约定 |
| 状态最大维度 | `20` | 保持现有 padding 约定 |
| 动作最大维度 | `20` | 核对实际 14 维动作及 padding |
| 图像视角数 | `3` | 保持三相机约定 |
| Chunk 长度 | `32` | 初版参考现有值 |
| 每次执行动作数 | `32` | 与现有部署执行策略核对 |
| 精度 | `bfloat16` | 按训练设备支持情况确认 |
| Batch size | `8` | 按实际显存测量调整 |
| 总步数 | `30000` | 仅参考，按数据量和评估结果确定 |

保留现有状态、动作顺序：

```text
0..5   左臂六个关节
6      左夹爪
7..12  右臂六个关节
13     右夹爪
```

必须另外核对动作是绝对关节位置还是增量、角度单位、夹爪开合方向和数值范围，不能只凭维度一致判定数据兼容。

### 4.3 用户可配置的路径

- `policy.path`：本地官方原始 X-VLA checkpoint。
- `policy.tokenizer_name`：实际可加载的 tokenizer 路径或标识。
- `dataset.root`：本地训练数据集目录。
- `output_dir`：独立 LoRA 训练输出目录。
- 合并导出目录：独立完整模型目录。

路径原则：

1. 不硬编码开发机器或训练机器的绝对路径。
2. 本地基座应包含当前 X-VLA 加载器所需的配置和权重；若为上游原始格式，先验证或转换兼容性。
3. 不假定 tokenizer 必然位于基座的 `tokenizer/` 子目录。
4. 相对路径的解析基准必须明确，并在启动日志中打印解析后的路径。
5. 建议普通训练采用“显式启动参数优先于 YAML”的规则，不再让自动发现目录覆盖用户配置。
6. 断点续训使用 checkpoint 保存的训练配置，并限制会破坏模型或优化器兼容性的覆盖。

### 4.4 LoRA 初始配方

以此前讨论的官方训练配方作为待对齐基线：

| 项目 | 计划初值 |
| --- | --- |
| 方法 | LoRA |
| Rank | `8` |
| Alpha | `16` |
| Bias | `none` |
| 目标模块 | `all-linear` |
| 额外完整训练 | Soft prompt、动作编码器、动作解码器 |

参考上游文件：`2toinf/X-VLA` 仓库的 `peft_train.py`。

实施时应固定所参考的上游提交，核对准确配方和默认行为，避免将本方案描述当作永远不变的上游接口。

本地关键模块：

```text
model.transformer.soft_prompt_hub
model.transformer.action_encoder
model.transformer.action_decoder
```

上面是本地路径核对目标，最终传入配置的路径应根据实际 `named_modules()` 和 PEFT 匹配规则确认。

本地通用包装会将 `full_training_modules` 转换为 PEFT 的 `modules_to_save`。优先复用该接口，不重复实现 adapter 注入。

注意事项：

- `all-linear` 不代表所有模型参数都训练，只表示对符合匹配规则的线性层添加适配器。
- 必须打印实际注入模块，确认 VLM 和动作 Transformer 的覆盖范围。
- 动作 Transformer 使用融合的 `attn.qkv`，不能照搬仅匹配 `q_proj/v_proj` 的配置。
- 动作编码器和解码器是基于 Embedding 的 `DomainAwareLinear`，需验证完整训练包装后的调用与保存兼容性。
- 通用 PEFT 包装会冻结原参数，因此不能仅依赖原来的 `train_soft_prompts: true`。
- 所有额外训练参数必须进入 adapter 保存和合并产物。

## 5. 独立、可选的调度策略

### 5.1 行为边界

- 未启用 LoRA 时，保持现有参数分组、学习率和调度行为。
- 启用 LoRA 但未选择专用调度时，不应隐式改变全量训练预设。
- 显式选择专用调度后，启用本节分组和阶段控制。
- LoRA 专用配置用于非 LoRA 训练时，应报出清晰配置错误。

### 5.2 参数分组

建议四组：

| 参数组 | 内容 |
| --- | --- |
| VLM LoRA | 视觉语言骨干中的 adapter 参数 |
| 动作侧 LoRA | 动作 Transformer 等其余 adapter 参数 |
| Soft prompt | 完整训练的 soft prompt |
| 动作输入输出 | 完整训练的动作编码器、解码器 |

必须在 PEFT 包装后按实际参数名称和归属分组，验证：

- 每个可训练参数恰好进入一个组。
- 冻结的基座参数不进入更新集合。
- 包装产生的额外模块副本没有被漏掉或重复加入。
- 明确预期之外的可训练参数应触发检查失败。

### 5.3 两阶段训练

阶段一：适配阶段。

- Soft prompt、动作编码器和解码器更新。
- VLM 和动作侧 LoRA 的学习率为 0。
- 适配阶段步数可配置；`1000` 作为官方配方对齐的候选初值。
- 设为 `0` 时跳过该阶段。

阶段二：联合训练。

- 启用 LoRA 参数更新。
- Soft prompt 和动作输入输出模块继续训练。
- 各组学习率及比例显式配置。
- 是否启用 warmup 和余弦衰减独立配置，不默认等同于官方始终启用。

实现约束：

- 调度按优化器更新步数计数，而不是按 micro-batch 数计数。
- 梯度累积和分布式训练不能改变阶段切换含义。
- 学习率为 0 与 `requires_grad=False` 的行为不同，首版需明确采用哪种机制。
- 全部组从训练开始就按固定顺序建立，以降低阶段切换和恢复状态的复杂度。
- 记录并验证冻结阶段的优化器状态行为，不能只检查参数是否变化。
- 日志显示当前阶段及每组实际学习率。
- 续训必须恢复原阶段与步数，不能重新进入适配阶段。

## 6. 训练启动与恢复

### 6.1 启动方式

建议增加独立 LoRA 启动入口，或者在现有入口上增加显式 LoRA 选择。实施时按现有脚本结构选择最小改动方案。

入口需要支持：

- LoRA 配置路径。
- 本地基座路径。
- 数据集路径。
- 独立输出目录。
- 设备、batch size、总训练步数。
- 断点恢复路径。
- 现有数据集校验能力。

本方案不提供尚未实现的可执行训练命令。

### 6.2 三种操作必须区分

| 操作 | 模型初始化 | 优化器与调度状态 |
| --- | --- | --- |
| 首次 LoRA 训练 | 官方原始基座 + 新 adapter | 新建 |
| LoRA 断点续训 | 同一基座 + 已保存 adapter | 恢复 |
| 部署合并导出 | 同一基座 + 已保存 adapter | 不需要加载 |

启动时应输出基座、数据集、训练模式、adapter 配置、可训练参数数量和参数组摘要，便于发现路径或冻结配置错误。

## 7. 产物保存与基座追踪

已接入本地 X-VLA PEFT 训练的基座清单 `xvla_base_manifest.json`：

- 开始训练时计算原始基座 `config.json`、权重及分片索引的 SHA-256 和大小；保存绝对来源路径，但身份比较不依赖该路径。
- 保存 tokenizer 来源及本地 tokenizer 文件哈希；不自动复制 tokenizer，部署导出时仍需确保相关资源可用。
- 记录 Python、关键依赖版本、Git commit 和工作区是否存在未提交修改。Git 不可访问时记录 `null`，不伪造代码版本。
- 每次保存 checkpoint 时复用初始化的基座身份，并生成当前模型产物目录的文件大小和哈希清单。
- 加载带清单的 X-VLA adapter 前重新计算实际基座身份；配置、权重或索引不匹配时拒绝加载。相同基座搬迁后允许匹配，但加载路径仍需通过 adapter 配置正确提供。
- 历史 adapter 没有清单时明确警告，不能提供追溯验证；远程基座暂不生成本地文件清单。
- 沿用现有 adapter、模型配置、训练配置、处理器及 training_state 保存机制，不改变全量模型保存方式。

实现文件为 `src/lerobot/common/xvla_provenance.py`，已接入训练初始化、checkpoint 保存和 adapter 加载。标准库测试覆盖身份比较、搬迁、篡改、历史兼容及文件清单；真实 PEFT 额外模块保存、完整续训和多进程集成仍需训练环境验证。

LoRA 训练产物应包含或明确关联：

- Adapter 权重和 adapter 配置。
- Soft prompt、动作编码器、动作解码器的训练后权重。
- 完整训练配置。
- 模型输入输出特征定义。
- 预处理、后处理配置以及其引用的统计量。
- Tokenizer 的来源或可离线加载文件。
- 基座来源、配置与权重标识。
- 训练代码版本及关键依赖版本。
- 续训所需的优化器、调度器、步数和必要随机状态。

优先复用现有 checkpoint 保存约定，仅补齐验证后确认缺失的部分。

不能仅记录一个可能被移动、覆盖或删除的绝对路径作为基座身份。

## 8. 合并导出设计

已新增 `scripts/xtrainer/merge_xvla_lora.py`。工具实现完成，真实模型合并仍需在具备 torch、PEFT 和实际 checkpoint 的环境验收，不能把文件路径测试视为模型验证通过。

```bash
python scripts/xtrainer/merge_xvla_lora.py \
  --base-model /data/original-xvla \
  --adapter /data/lora/checkpoints/last/pretrained_model \
  --output-dir /data/exports/xvla-merged \
  --validation-batch /data/validation/observation.safetensors \
  --device cuda
```

`--validation-batch` 是使用训练处理器得到的 Tensor 字典，通过 safetensors 保存。必须包含模型实际使用的图像、状态、语言 token 和正确的 domain_id；不是原始相机文件或任意 pickle。建议使用留出的真实观测。

工具要求有效基座清单及完整的处理器文件，不静默降级为无身份验证导出。输出目录必须不存在且不能与输入目录重叠。合并前检查已记录的 adapter 文件哈希，合并时使用 `safe_merge=True` 并逐张量检查额外训练模块。

默认保留配置精度，可用 `--dtype float32` 或 `--dtype bfloat16` 指定；默认输出比较容限为 `atol=0.001`、`rtol=0.01`，实际容限需要结合精度和动作单位审查。预测前重置策略和随机种子，因为当前 X-VLA 的 noise 参数不会直接控制生成噪声。

输出包含完整权重、策略配置、处理器和 tokenizer，以及 `merge_report.json`。原基座清单作为来源记录保留，其中 artifacts 描述的是源 adapter 而非新模型。不会把 adapter 的训练配置伪装成完整模型续训配置，也不会复制训练优化器状态。

任何保存后的验证失败都会留下 `EXPORT_FAILED.txt`；此目录不得部署，也不会自动删除用户文件。成功必须同时通过合并前后输出、额外模块权重、严格重新加载和处理器重新加载检查。

限制：当前模型加载器使用单个 `model.safetensors`；工具按此契约要求本地基座。导出时同时保留合并模型和重载模型，需为校验预留额外内存。策略 fallback 的 tokenizer 路径记录为导出绝对路径；迁移目录时应使用带相对 tokenizer 资源的保存处理器，不应退回旧路径。

已新增独立合并工具：

```text
scripts/xtrainer/merge_xvla_lora.py
```

接口及当前验证边界见本节开头。

输入：

- 官方原始基座目录。
- LoRA checkpoint / adapter 目录。
- 新的完整模型输出目录。
- 可选导出精度。

处理步骤：

1. 校验输出目录，拒绝覆盖基座、adapter 和已有全量模型。
2. 核对基座身份、模型结构和 adapter 配置。
3. 按训练时一致的配置加载基座，包括动作模式和输入输出结构。
4. 加载 adapter 及额外训练模块。
5. 在固定输入和随机状态下记录未合并模型的输出。
6. 合并并移除 PEFT 包装，正确保留额外训练模块。
7. 导出为现有 `XVLAPolicy.from_pretrained` 可读取的完整格式。
8. 确保导出配置不再错误声明必须加载 PEFT。
9. 保存匹配的预处理、后处理和 tokenizer 相关资源。
10. 重新从导出目录加载并比较输出。

验收要求：

- 不只是产生权重文件，而是真正能由现有部署加载器读取。
- 导出后不依赖 adapter 目录即可完成部署加载。
- 无缺失权重、意外权重或静默随机初始化。
- 推理比较使用 eval 模式、固定噪声及相同输入。
- 误差容限按实际精度确定；不能要求 BF16 合并一定逐位相等。

## 9. 复用现有真机部署

代码接入已完成，真实 checkpoint 启动及真机任务验收待进行；本次没有启动服务或连接机器人。

沿用 `configs/xtrainer/deploy.yaml`，通过现有 CLI 覆盖模型路径，无需新增 LoRA 部署配置：

```bash
python scripts/xtrainer/serve_policy.py \
  --config configs/xtrainer/deploy.yaml \
  --checkpoint /data/exports/xvla-merged \
  --device cuda
```

也可以修改 YAML 的 `policy.checkpoint`；CLI 优先于 YAML。只指定合并后的完整模型目录，不填写基座或 adapter 路径。服务启动后，机器人客户端继续使用原来的连接与执行方式，不增加热切换。

新增启动前检查：

- 拒绝含 `adapter_config.json` 的独立 adapter 和含 `EXPORT_FAILED.txt` 的失败导出。
- 对合并产物要求成功的 `merge_report.json`、完整权重、配置、处理器和 tokenizer。
- 对合并产物禁止处理器加载失败后回退默认处理器，避免误用预处理约定。
- 原全量 checkpoint 没有合并标记时继续原加载行为；原域编号、动作模式及动作维度校验继续生效。

检查不是模型质量或物理安全认证，也不能替代真实观测测试。现有 safety 配置包含无限动作增量/关节范围，本次不改其行为；首次真机运行前必须自行核对适用限幅、急停和人工接管条件。

已确认：部署加载方式与现有全量模型一致。服务启动时加载一个完整 checkpoint；切换模型时修改 checkpoint 路径并重启服务。

LoRA 必须先与训练时的原始基座合并导出，部署端只读取导出的完整模型。不增加“启动时加载基座 + 独立 adapter”的分支，也不增加运行中的模型或 adapter 热切换功能。

预期部署操作：

1. 保留已有全量模型 B 的配置和路径。
2. 将完整模型 C 放到独立目录。
3. 通过现有部署配置中的模型路径字段选择 C。
4. 保持机器人控制、相机采集、动作执行及安全限制逻辑不变。
5. 验证加载及离线输出后再进入低速真机测试。

部署检查：

- 相机名称、顺序、尺寸与图像变换一致。
- 状态和动作维度、顺序、单位、夹爪方向一致。
- `domain_id`、动作模式、chunk 配置与导出配置一致。
- 预处理和后处理没有误用已有全量模型的统计文件。
- 无 NaN、Inf 或明显异常幅度。
- 保留现有动作限幅、急停和人工接管机制。

如果加载兼容性需要修正，优先修正导出工具，而不是重写已经跑通的真机执行逻辑。

## 10. 测试与验收

### 10.1 配置与回归测试

- 原全量配置仍可解析。
- 原全量启动方式不需要新增必填参数。
- 未启用 LoRA 时，优化器和调度行为不变。
- LoRA 配置能正确映射到 PEFT。
- 显式基座路径不会被默认本地路径覆盖。
- 不存在的路径或不兼容基座给出明确错误。

### 10.2 模型与梯度测试

- 用小模型或轻量配置验证模块注入。
- LoRA 和额外训练模块有预期梯度。
- 冻结基座不被更新。
- 不要求零初始化条件下所有 LoRA 因子在第一次反向中都出现非零梯度。
- Soft prompt 和动作模块被正确保存。
- 可训练参数没有遗漏或重复分组。

### 10.3 调度测试

- 适配阶段前后学习率符合设计。
- 覆盖切换前一步、切换点、切换后一步。
- 覆盖 `0` 步适配及关闭余弦调度。
- 覆盖梯度累积下的更新步数。
- 覆盖阶段切换前后的断点恢复。
- 与不中断训练对比恢复后的学习率轨迹。

### 10.4 保存、合并与加载测试

- 保存重载后的 adapter 输出一致。
- 额外训练模块不丢失。
- 错误基座能被识别并拒绝。
- 合并前后输出误差在约定容限内。
- 导出模型可用现有 X-VLA 加载入口读取。
- 不依赖 adapter 目录。
- 预处理和后处理资源完整。

### 10.5 实际训练与评估

建议顺序：

1. 少量 batch 的训练冒烟测试。
2. 少量完整轨迹的拟合检查。
3. 小规模实验跨过阶段切换点。
4. 保存、恢复、合并、重新加载的闭环验证。
5. 正式训练和独立轨迹离线评估。
6. 低速真机测试。
7. 与已有全量模型 B 对照任务成功率和异常行为。

验证集按完整 episode 划分，避免同一轨迹相邻帧同时出现在训练集和验证集。

记录 loss、各参数组学习率、梯度、显存和吞吐；模型效果最终由任务表现评估，不能仅依赖训练 loss。

## 11. 实施顺序与交付物

| 阶段 | 工作 | 完成标准 |
| --- | --- | --- |
| 1 | 检查当前 PEFT 配置、保存及恢复链路 | 明确可复用能力和真实缺口 |
| 2 | 新增独立 LoRA 配置与路径规则 | 可解析，路径优先级明确，全量流程不变 |
| 3 | 接入目标模块与额外训练模块 | 注入、梯度及保存测试通过 |
| 4 | 实现独立分组与可选调度 | 边界和恢复测试通过 |
| 5 | 接入训练启动及短训练验证 | 实际 checkpoint 可保存和恢复 |
| 6 | 实现合并导出工具 | 现有加载器可读取且输出一致 |
| 7 | 离线及真机验证 | 形成可对照结果，保留回退路径 |

计划交付：

- 独立 LoRA YAML 配置。
- 可选 LoRA 优化器分组及调度实现。
- 明确的训练启动和恢复接口。
- 合并导出工具。
- 针对性测试和全量训练回归验证。
- 实际可执行的训练、恢复、导出和部署操作说明。

不在第一版范围：

- 自行实现 LoRA 数学算子。
- 重写通用训练循环。
- QLoRA 或量化部署。
- 真机端动态 adapter 热切换。
- 无关模型重构及其他策略的训练行为修改。

## 12. 实施前待补充信息

- 训练机器上的原始基座路径及 checkpoint 格式。
- Tokenizer 实际位置。
- 数据集路径、轨迹数量、任务数量和动作语义。
- GPU 型号、数量及每卡显存。
- 当前全量模型部署时使用的配置和加载方式。
- 首轮适配阶段步数及是否启用余弦调度。
- 验证集划分和任务成功判定标准。

这些信息用于填写实际配置和确定实验规模，不应被写成不可调整的代码常量。

## 13. 完成定义

只有同时满足以下条件，才算完成 LoRA 接入：

- [ ] 从用户指定的本地官方原始基座开始训练。
- [ ] LoRA 与额外模块的训练范围正确。
- [ ] 独立调度可选，原全量训练不受影响。
- [ ] 可正确保存并断点续训。
- [ ] 可合并导出独立完整模型。
- [ ] 合并前后及重新加载后的输出通过一致性验证。
- [ ] 可使用现有真机部署代码加载。
- [ ] 保留已有全量模型及回退方式。
- [ ] 完成实际任务评估并记录结果。
